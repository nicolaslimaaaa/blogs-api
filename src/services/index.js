const userService = require('./user');
const categoryService = require('./category');
const postService = require('./post');

module.exports = {
    userService,
    categoryService,
    postService,
};